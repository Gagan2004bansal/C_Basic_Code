#include<stdio.h>
#include<math.h>
/* && -> and
   || -> or
   !  -> not  */
   int main(){
    int a,b;
    a = 7;
    b = 4;

    printf("Logic operators is %d", (a<5 && b<6));
    return 0;
   }